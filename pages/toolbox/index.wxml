<view class="page">
  <text class="hero-title">吗喽的百宝箱</text>
  <text class="subtext">这里先放几个工具入口。0.1 先做占位，不急着把世界修完。</text>

  <view class="tool-grid">
    <view wx:for="{{items}}" wx:key="*this" class="tool-card" bindtap="showComingSoon">
      <text>{{item}}</text>
      <text class="subtext">0.2 上线</text>
    </view>
  </view>
</view>
